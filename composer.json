{
    "name": "snipershady/typeidentifier",
    "description": "Free service to identify primitive type of a variable",
    "keywords": [
        "strict type",
        "type",
        "primitive"
    ],
    "homepage": "https://www.spinfo.it",
    "type": "library",
    "license": "GPL-2.0-only",
    "minimum-stability": "stable",
    "prefer-stable": true,
    "authors": [
        {
            "name": "Stefano Perrini",
            "email": "perrini.stefano@gmail.com",
            "homepage": "https://www.spinfo.it"
        }
    ],
    "autoload": {
        "psr-4": {
            "TypeIdentifier\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "TypeIdentifier\\Tests\\": "tests/"
        }
    },
    "require": {
        "php": "^8.3"
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "^3.95.23",
        "phpstan/phpstan": "^2.2.9",
        "phpstan/phpstan-phpunit": "^2.0.18",
        "phpstan/phpstan-strict-rules": "^2.0.12",
        "phpunit/phpunit": "^12.5.34",
        "rector/rector": "^2.6.4"
    },
    "scripts": {
        "test": "vendor/bin/phpunit",
        "rector": "vendor/bin/rector process",
        "rector-dry": "vendor/bin/rector process --dry-run",
        "cs-fix": "vendor/bin/php-cs-fixer fix",
        "cs-check": "vendor/bin/php-cs-fixer fix --dry-run --diff",
        "phpstan": "vendor/bin/phpstan analyse",
        "quality": [
            "@rector",
            "@cs-fix"
        ],
        "quality-check": [
            "@rector-dry",
            "@cs-check",
            "@phpstan"
        ],
        "post-install-cmd": [
            "echo \"\n✅ Dependencies installed. Run 'composer quality' to apply code quality fixes.\n\""
        ],
        "post-update-cmd": [
            "echo \"\n✅ Dependencies updated. Run 'composer quality' to apply code quality fixes.\n\""
        ],
        "test:unit": "vendor/bin/phpunit --testsuite unit",
        "test:integration": "vendor/bin/phpunit --testsuite integration"
    },
    "scripts-descriptions": {
        "test": "Run the whole PHPUnit suite (integration tests skip without a live endpoint)",
        "rector": "Run Rector to refactor code",
        "rector-dry": "Run Rector in dry-run mode (preview changes without applying)",
        "cs-fix": "Fix code style issues with PHP-CS-Fixer",
        "cs-check": "Check code style without making changes",
        "phpstan": "Run PHPStan static analysis",
        "quality": "Run all quality tools (Rector + PHP-CS-Fixer)",
        "quality-check": "Check code quality without making changes (dry-run mode)",
        "test:unit": "Run the unit suite only: no I/O, runnable anywhere",
        "test:integration": "Run the integration suite against TYPEIDENTIFIER_ENDPOINT (defaults to the docker-compose endpoint)"
    },
    "config": {
        "optimize-autoloader": true,
        "sort-packages": true,
        "preferred-install": "dist"
    }
}
